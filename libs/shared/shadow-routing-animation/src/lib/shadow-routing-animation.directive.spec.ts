import { ShadowRoutingAnimationDirective } from './shadow-routing-animation.directive';

describe('AnimateOnRoutingDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowRoutingAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
