import { LightRoutingAnimationHostDirective } from './light-routing-animation-host.directive'
describe('LightRoutingAnimationHostDirective', () => {
  it('should create an instance', () => {
    const directive = new LightRoutingAnimationHostDirective();
    expect(directive).toBeTruthy();
  });
});
